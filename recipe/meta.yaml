{% set name = "ndarray-listener" %}
{% set version = "1.0.26" %}
{% set sha256 = "44a18bba0a6353440f7831782e01ede253078144d17b8b8a48a8936347e1538d" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools
    - pytest-runner
  run:
    - python
    - numpy >=1.10

test:
  requires:
    - pytest >=3
    - pytest-pep8
  imports:
    - ndarray_listener
  commands:
    - python -c "import sys; import ndarray_listener; sys.exit(ndarray_listener.test())"

about:
  home: https://github.com/limix/ndarray-listener
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  summary: 'Implementation of the Observer pattern for NumPy arrays.'
  doc_url: https://github.com/limix/ndarray-listener
  dev_url: https://github.com/limix/ndarray-listener

extra:
  recipe-maintainers:
    - Horta
